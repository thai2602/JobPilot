# Copy this file to frontend/.env.local for local development.
# NEXT_PUBLIC_* values are visible in the browser; never put secrets here.

NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_GOOGLE_CLIENT_ID=replace_with_google_web_client_id
